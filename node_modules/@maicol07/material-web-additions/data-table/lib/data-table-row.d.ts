import { LitElement, PropertyValueMap } from 'lit';
import { DataTableCell } from './data-table-cell.js';
export interface RowCheckedEventDetail {
    selected: boolean;
}
export declare class DataTableRow extends LitElement {
    /**
     * Whether the row is selected.
     */
    selected: boolean;
    /**
     * Overall height of the table. Available in three different measures.
     */
    density?: '' | 'tight' | 'comfortable' | 'dense' | 'compact';
    /**
     * Cells of the row.
     * @internal
     */
    cells: DataTableCell[];
    /**
     * Cells with type `checkbox`.
     *
     * @internal
     */
    protected checkboxCells: DataTableCell[];
    connectedCallback(): void;
    /** @internal */
    get checkboxCell(): DataTableCell | undefined;
    render(): import("lit-html").TemplateResult<1>;
    /** @internal */
    onCheckboxClicked: (e: Event) => void;
    protected updated(_changedProperties: PropertyValueMap<any> | Map<PropertyKey, unknown>): void;
}
